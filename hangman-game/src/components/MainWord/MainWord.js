import React from "react";
import "./MainWord.css";

function MainWord() {
  // let words = ["kavin", "aswin", "subhash", "vineeth"];
  // let guessed="aswin"
  // function randomWord() {
  //    let randomWord = words[Math.floor(Math.random() * words.length)];
  //    let wordStatus = randomWord.split('').map(letter => (guessed.indexOf(letter) >= 0 ? letter : " _ ")).join('');
  //    console.log(wordStatus)
      
  // }
  return <div>{}</div>;
}

export default MainWord;
